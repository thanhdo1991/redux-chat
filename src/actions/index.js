export const addMessage = (item) => ({
  type: 'ADD_MESSAGE',
  item
})

export const addImage = (item) => ({
  type: 'ADD_IMAGE',
  item
})

export const toggleLogin = () => ({
  type: 'TOGGLE_LOGIN'
})
